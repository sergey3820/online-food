import React, { FC } from "react";
import styles from "./MeatPizzas.module.scss";

const MeatPizzas:FC = () => {
    return (
      <div className={styles.main}>
        <h1>Meat Pizzas</h1>
      </div>
    );
}

export default MeatPizzas;