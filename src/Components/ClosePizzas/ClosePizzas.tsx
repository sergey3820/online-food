import React, { FC } from "react";
import styles from "./ClosePizzas.module.scss";

const ClosePizzas:FC = () => {
    return (
      <div className={styles.main}>
          <h1>Close Pizzas</h1>
      </div>
    );
}

export default ClosePizzas;